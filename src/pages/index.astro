---
import Layout from '../layouts/Layout.astro';
import { supabase } from '../lib/supabase';
import { Image } from 'astro:assets';

// Fetch últimos 5 posts
const { data: recentPosts } = await supabase
  .from('posts')
  .select('*')
  .order('created_at', { ascending: false })
  .limit(5);
---

<Layout title="Mi Blog - Inicio" description="Últimos posts y blog increíble">
  <!-- Hero Section -->
  <section class="relative bg-gradient-to-br from-blue-600 via-purple-600 to-indigo-800 text-white overflow-hidden py-32 px-4">
    <div class="absolute inset-0 bg-black/20"></div>
    <div class="relative max-w-6xl mx-auto text-center">
      <h1 class="text-6xl md:text-7xl font-black mb-8 bg-gradient-to-r from-white to-gray-200 bg-clip-text text-transparent drop-shadow-2xl">
        Bienvenido a<br><span class="text-5xl md:text-6xl text-yellow-300">Mi Blog</span>
      </h1>
      <p class="text-xl md:text-2xl mb-12 max-w-3xl mx-auto opacity-90 leading-relaxed">
        Descubre contenido increíble. Lee los últimos posts o{' '}
        <a href="/login" class="font-bold underline hover:no-underline">inicia sesión</a>{' '}
        para más funciones.
      </p>
      
      <!-- CTA Buttons -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center max-w-2xl mx-auto">
        <a 
          href="/blog" 
          class="group bg-white/20 backdrop-blur-sm text-white font-bold py-5 px-10 rounded-2xl text-lg hover:bg-white/30 transition-all duration-500 hover:scale-105 shadow-2xl hover:shadow-white/20"
        >
          <span class="group-hover:-translate-x-2 transition-transform">Ver Posts</span>
        </a>
        <a 
          href="/login"
          class="group bg-gradient-to-r from-yellow-400 to-orange-500 hover:from-yellow-500 hover:to-orange-600 text-gray-900 font-bold py-5 px-10 rounded-2xl text-lg shadow-xl hover:shadow-2xl hover:scale-105 transition-all duration-500"
        >
          Iniciar Sesión →
        </a>
      </div>
    </div>
  </section>

  <!-- Featured Posts -->
  {recentPosts && recentPosts.length > 0 && (
    <section class="py-32 px-4 bg-white">
      <div class="max-w-6xl mx-auto">
        <div class="text-center mb-20">
          <h2 class="text-4xl md:text-5xl font-black text-gray-900 mb-6">
            Últimos Posts
          </h2>
          <p class="text-xl text-gray-600 max-w-2xl mx-auto">
            Los 5 posts más recientes, actualizados en tiempo real.
          </p>
        </div>
        
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
          {recentPosts.map((post) => (
            <article 
              key={post.id}
              class="group bg-gradient-to-br from-white to-gray-50 rounded-3xl shadow-xl hover:shadow-2xl transition-all duration-500 overflow-hidden hover:-translate-y-2 border border-gray-100 hover:border-gray-200"
            >
              <div class="p-8 space-y-6">
                <div class="flex items-center text-sm text-gray-500 space-x-2">
                  <time>{new Date(post.created_at).toLocaleDateString('es-ES')}</time>
                  <span>•</span>
                  <span>{Math.floor(Math.random() * 10) + 1} min lectura</span>
                </div>
                
                <h3 class="text-2xl font-bold text-gray-900 group-hover:text-blue-600 transition-colors line-clamp-2 h-14">
                  {post.title}
                </h3>
                
                <p class="text-gray-600 leading-relaxed line-clamp-3 h-20">
                  {post.content}
                </p>
                
                <a 
                  href={`/blog/${post.id}`}
                  class="inline-flex items-center text-blue-600 font-semibold hover:text-blue-700 group/link"
                >
                  Leer artículo
                  <svg class="w-5 h-5 ml-1 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
                  </svg>
                </a>
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>
  )}

  <!-- CTA Final -->
  <section class="py-24 px-4 bg-gradient-to-r from-blue-600 to-indigo-700 text-white text-center">
    <div class="max-w-4xl mx-auto">
      <h2 class="text-4xl md:text-5xl font-black mb-6">¿Listo para unirte?</h2>
      <p class="text-xl mb-8 opacity-90">Únete a la conversación. Crea tu cuenta hoy.</p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="/login" class="bg-white text-blue-600 font-bold py-5 px-10 rounded-2xl hover:bg-gray-100 transition-all duration-300 text-lg shadow-xl hover:shadow-2xl">
          Iniciar Sesión
        </a>
        <a href="/signup" class="border-2 border-white/50 text-white font-bold py-5 px-10 rounded-2xl hover:bg-white/10 backdrop-blur-sm transition-all duration-300 text-lg">
          Crear Cuenta
        </a>
      </div>
    </div>
  </section>

  <style>
    .line-clamp-2 {
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
    .line-clamp-3 {
      display: -webkit-box;
      -webkit-line-clamp: 3;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
  </style>
</Layout>
